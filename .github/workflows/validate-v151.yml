name: Validate MyWeather 1.5.1
on:
  push:
    branches:
      - main
    paths:
      - validate-v151.ready
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test
      - run: npm run check
