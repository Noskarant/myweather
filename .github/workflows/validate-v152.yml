name: Validate MyWeather 1.5.2
on:
  push:
    branches:
      - main
    paths:
      - validate-v152.ready
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test
      - run: npm run check
